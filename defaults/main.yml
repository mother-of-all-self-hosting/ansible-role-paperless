# SPDX-FileCopyrightText: 2023 - 2024 Nikita Chernyi
# SPDX-FileCopyrightText: 2023 - 2025 Slavi Pantaleev
# SPDX-FileCopyrightText: 2024 Julian-Samuel Gebühr
# SPDX-FileCopyrightText: 2024 Sergio Durigan Junior
# SPDX-FileCopyrightText: 2025 MASH project contributors
# SPDX-FileCopyrightText: 2025 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---

# Project source code URL: https://github.com/paperless-ngx/paperless-ngx

paperless_enabled: true

paperless_identifier: paperless
paperless_base_path: "/{{ paperless_identifier }}"
paperless_consume_path: "{{ paperless_base_path }}/consume"
paperless_data_path: "{{ paperless_base_path }}/data"
paperless_export_path: "{{ paperless_base_path }}/export"
paperless_media_path: "{{ paperless_base_path }}/media"
paperless_container_consume_path: "/usr/src/paperless/consume"
paperless_container_data_path: "/usr/src/paperless/data"
paperless_container_export_path: "/usr/src/paperless/export"
paperless_container_media_path: "/usr/src/paperless/media"

# renovate: datasource=docker depName=ghcr.io/paperless-ngx/paperless-ngx versioning=semver
paperless_version: "2.20.11"

paperless_uid: ""
paperless_gid: ""

# The scheme used for serving Paperless-ngx.
paperless_scheme: https

# The hostname at which Paperless-ngx is served.
paperless_hostname: ""

# The path at which Paperless-ngx is served.
paperless_path_prefix: /

paperless_container_image: "{{ paperless_container_image_registry_prefix }}paperless-ngx/paperless-ngx:{{ paperless_container_image_tag }}"
paperless_container_image_tag: "{{ paperless_version }}"
paperless_container_image_registry_prefix: "{{ paperless_container_image_registry_prefix_upstream }}"
paperless_container_image_registry_prefix_upstream: "{{ paperless_container_image_registry_prefix_upstream_default }}"
paperless_container_image_registry_prefix_upstream_default: ghcr.io/
paperless_container_image_force_pull: "{{ paperless_container_image.endswith(':latest') }}"

# The base container network. It will be auto-created by this role if it doesn't exist already.
paperless_container_network: "{{ paperless_identifier }}"

# Control if the container network is deleted on uninstalling.
paperless_container_network_deletion_enabled: true

# Specify how the container publishes its HTTP port (as defined by `paperless_container_http_port`).
#
# Takes an "<ip>:<port>" or "<port>" value (e.g. "127.0.0.1:2586"), or empty string to not expose.
paperless_container_http_host_bind_port: ""

# The port number in the container
paperless_container_http_port: 8000

# A list of additional container networks that the container would be connected to.
# The role does not create these networks, so make sure they already exist.
# Use this to expose this container to another reverse proxy, which runs in a different container network.
paperless_container_additional_networks: "{{ paperless_container_additional_networks_auto + paperless_container_additional_networks_custom }}"
paperless_container_additional_networks_auto: []
paperless_container_additional_networks_custom: []

# A list of additional "volumes" to mount in the container.
# This list gets populated dynamically at runtime. You can provide a different default value,
# if you wish to mount your own files into the container.
# Contains definition objects like this: `{"type": "bind", "src": "/outside", "dst": "/inside", "options": "readonly"}.
# See the `--mount` documentation for the `docker run` command.
paperless_container_additional_volumes: "{{ paperless_container_additional_volumes_auto + paperless_container_additional_volumes_custom }}"
paperless_container_additional_volumes_auto: []
paperless_container_additional_volumes_custom: []

# paperless_container_labels_traefik_enabled controls whether labels to assist a Traefik reverse-proxy will be attached to the container.
# See `../templates/labels.j2` for details.
#
# To inject your own other container labels, see `paperless_container_labels_additional_labels_custom`.
paperless_container_labels_traefik_enabled: true
paperless_container_labels_traefik_docker_network: "{{ paperless_container_network }}"
paperless_container_labels_traefik_hostname: "{{ paperless_hostname }}"
# The path prefix must either be `/` or not end with a slash (e.g. `/paperless`).
paperless_container_labels_traefik_path_prefix: "{{ paperless_path_prefix }}"
paperless_container_labels_traefik_rule: "Host(`{{ paperless_container_labels_traefik_hostname }}`){% if paperless_container_labels_traefik_path_prefix != '/' %} && PathPrefix(`{{ paperless_container_labels_traefik_path_prefix }}`){% endif %}"
paperless_container_labels_traefik_priority: 0
paperless_container_labels_traefik_entrypoints: web-secure
paperless_container_labels_traefik_tls: "{{ paperless_container_labels_traefik_entrypoints != 'web' }}"
paperless_container_labels_traefik_tls_certResolver: default  # noqa var-naming

# Controls which additional headers to attach to all HTTP requests.
# To add your own custom request headers, use `paperless_container_labels_traefik_additional_request_headers_custom`
paperless_container_labels_traefik_additional_request_headers: "{{ paperless_container_labels_traefik_additional_request_headers_auto | combine(paperless_container_labels_traefik_additional_request_headers_custom) }}"
paperless_container_labels_traefik_additional_request_headers_auto: {}
paperless_container_labels_traefik_additional_request_headers_custom: {}

# Controls which additional headers to attach to all HTTP responses.
# To add your own custom response headers, use `paperless_container_labels_traefik_additional_response_headers_custom`
paperless_container_labels_traefik_additional_response_headers: "{{ paperless_container_labels_traefik_additional_response_headers_auto | combine(paperless_container_labels_traefik_additional_response_headers_custom) }}"
paperless_container_labels_traefik_additional_response_headers_auto: |
  {{
    {}
    | combine ({'X-XSS-Protection': paperless_http_header_xss_protection} if paperless_http_header_xss_protection else {})
    | combine ({'X-Content-Type-Options': paperless_http_header_content_type_options} if paperless_http_header_content_type_options else {})
    | combine ({'Content-Security-Policy': paperless_http_header_content_security_policy} if paperless_http_header_content_security_policy else {})
    | combine ({'Permissions-Policy': paperless_http_header_permissions_policy} if paperless_http_header_permissions_policy else {})
    | combine ({'Strict-Transport-Security': paperless_http_header_strict_transport_security} if paperless_http_header_strict_transport_security and paperless_container_labels_traefik_tls else {})
  }}
paperless_container_labels_traefik_additional_response_headers_custom: {}

# paperless_container_labels_additional_labels contains a multiline string with additional labels to add to the container label file.
# See `../templates/labels.j2` for details.
# To add your own custom labels, use `paperless_container_labels_additional_labels_custom`
#
# Example:
# paperless_container_labels_additional_labels_custom:
#   - my.label=1
#   - another.label="here"
paperless_container_labels_additional_labels: "{{ paperless_container_labels_additional_labels_auto + paperless_container_labels_additional_labels_custom }}"
paperless_container_labels_additional_labels_auto: []
paperless_container_labels_additional_labels_custom: []

# Specify the size of the container's tmpfs volume (/tmp)
paperless_container_tmpfs_tmp_size: 10000m

# Specify the size of the container's tmpfs volume (/var/log/supervisord)
paperless_container_tmpfs_log_supervisord_size: 20m

# Specify the size of the container's tmpfs volume (/var/run/supervisord)
paperless_container_tmpfs_run_supervisord_size: 10m

# Specify the size of the container's tmpfs volume (/run)
paperless_container_tmpfs_run_size: 10m

# A list of extra arguments to pass to the container (`docker create` command)
paperless_container_extra_arguments: "{{ paperless_container_extra_arguments_auto + paperless_container_extra_arguments_custom }}"
paperless_container_extra_arguments_auto: []
paperless_container_extra_arguments_custom: []

# Specifies how often the container health-check will run.
#
# For Traefik based setups, it's important that the interval is short,
# because the interval value also specifies the "initial wait time".
# This is a Docker (moby) bug: https://github.com/moby/moby/issues/33410
# Without a successful healthcheck, Traefik will not register the service for reverse-proxying.
# Thus, the health interval determines the initial start-up time -- the smaller, the better.
#
# For non-Traefik setups, we use the default healthcheck interval (60s) to decrease overhead.
paperless_container_health_interval: "{{ '5s' if paperless_container_labels_traefik_enabled else '60s' }}"

# Controls if the filesystem of the container should be readonly.
#
# Enabling additional OCR languages requires installing additional packages on startup,
# which necessitates that the filesystem is not readonly.
paperless_container_readonly_enabled: "{{ paperless_ocr_languages == paperless_ocr_languages_default }}"

# Specifies the value of the `X-XSS-Protection` header
# Stops pages from loading when they detect reflected cross-site scripting (XSS) attacks.
#
# Learn more about it is here:
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-XSS-Protection
# - https://portswigger.net/web-security/cross-site-scripting/reflected
paperless_http_header_xss_protection: "1; mode=block"

# Specifies the value of the `X-Content-Type-Options` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-Content-Type-Options
paperless_http_header_content_type_options: nosniff

# Specifies the value of the `Content-Security-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy
paperless_http_header_content_security_policy: frame-ancestors 'self'

# Specifies the value of the `Permissions-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Permissions-Policy
paperless_http_header_permissions_policy: "{{ 'interest-cohort=()' if paperless_floc_optout_enabled else '' }}"

# Specifies the value of the `Strict-Transport-Security` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
paperless_http_header_strict_transport_security: "max-age=31536000; includeSubDomains{{ '; preload' if paperless_hsts_preload_enabled else '' }}"

# Controls whether to send a "Permissions-Policy interest-cohort=();" header along with all responses
#
# Learn more about what it is here:
# - https://www.eff.org/deeplinks/2021/03/googles-floc-terrible-idea
# - https://paramdeo.com/blog/opting-your-website-out-of-googles-floc-network
# - https://amifloced.org/
#
# Of course, a better solution is to just stop using browsers (like Chrome), which participate in such tracking practices.
# See: `paperless_http_header_permissions_policy`
paperless_floc_optout_enabled: true

# Controls if HSTS preloading is enabled
#
# In its strongest and recommended form, the [HSTS policy](https://www.chromium.org/hsts) includes all subdomains, and
# indicates a willingness to be "preloaded" into browsers:
# `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`
# For more information visit:
# - https://en.wikipedia.org/wiki/HTTP_Strict_Transport_Security
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
# - https://hstspreload.org/#opt-in
# See: `paperless_http_header_strict_transport_security`
paperless_hsts_preload_enabled: false

# List of systemd services that the Paperless-ngx systemd service depends on
paperless_systemd_required_services_list: "{{ paperless_systemd_required_services_list_default + paperless_systemd_required_services_list_auto + paperless_systemd_required_services_list_custom }}"
paperless_systemd_required_services_list_default: "{{ [devture_systemd_docker_base_docker_service_name] if devture_systemd_docker_base_docker_service_name else [] }}"
paperless_systemd_required_services_list_auto: []
paperless_systemd_required_services_list_custom: []

# List of systemd services that the Paperless-ngx systemd service wants
paperless_systemd_wanted_services_list: "{{ paperless_systemd_wanted_services_list_default + paperless_systemd_wanted_services_list_auto + paperless_systemd_wanted_services_list_custom }}"
paperless_systemd_wanted_services_list_default: []
paperless_systemd_wanted_services_list_auto: []
paperless_systemd_wanted_services_list_custom: []

# Controls how long to wait for the container to stop gracefully before killing it.
paperless_container_stop_grace_time_seconds: "{{ devture_systemd_docker_base_container_stop_grace_time_seconds }}"

# Controls how long the pre-start cleanup should wait for stale container-name release
# before failing startup explicitly.
paperless_container_cleanup_timeout_seconds: "{{ devture_systemd_docker_base_container_cleanup_timeout_seconds }}"

# Controls the delay between pre-start cleanup retries.
paperless_container_cleanup_retry_delay_seconds: "{{ devture_systemd_docker_base_container_cleanup_retry_delay_seconds }}"

# Shell command used in the systemd pre-start phase to deterministically remove a stale
# container and wait until Docker releases its name.
paperless_container_cleanup_command: |
  i=0;
  while [ "$i" -lt {{ paperless_container_cleanup_timeout_seconds }} ]; do
  {{ devture_systemd_docker_base_host_command_docker }} stop -t {{ paperless_container_stop_grace_time_seconds }} {{ paperless_identifier }} >/dev/null 2>&1 || true;
  {{ devture_systemd_docker_base_host_command_docker }} rm -f {{ paperless_identifier }} >/dev/null 2>&1 || true;
  if {{ devture_systemd_docker_base_host_command_docker }} container inspect {{ paperless_identifier }} >/dev/null 2>&1; then
  i=$((i + 1));
  sleep {{ paperless_container_cleanup_retry_delay_seconds }};
  continue;
  fi;
  if {{ devture_systemd_docker_base_host_command_docker }} info >/dev/null 2>&1; then
  exit 0;
  fi;
  echo "Failed to query Docker daemon while cleaning up {{ paperless_identifier }}" >&2;
  exit 1;
  done;
  echo "Failed to remove stale container {{ paperless_identifier }} after {{ paperless_container_cleanup_timeout_seconds }} attempts" >&2;
  exit 1

paperless_url: "{{ paperless_scheme }}://{{ paperless_hostname }}"
paperless_app_title: Paperless-ngx
paperless_secret_key: ""

paperless_admin_user: ""
paperless_admin_password: ""

# Valid values: debug, info, warning, error, critical
paperless_loglevel: error

# Specify database to use
# Valid values: mariadb, postgres, sqlite
paperless_database_type: postgres

paperless_database_postgres_hostname: ""
paperless_database_postgres_port: 5432
paperless_database_postgres_username: paperless
paperless_database_postgres_password: ""
paperless_database_mariadb_hostname: ""
paperless_database_mariadb_port: 3306
paperless_database_mariadb_username: paperless
paperless_database_mariadb_password: ""

# Use this to set the sslmode parameter of the SSL connection
# By default, we expect a local container (without SSL), so attempting SSL connections is not necessary.
paperless_database_postgres_sslmode: disable
paperless_database_mariadb_sslmode: DISABLED

paperless_database_name: paperless

paperless_redis_hostname: ""
paperless_redis_port: 6379
paperless_redis_database: 0
paperless_redis_url: "redis://{{ paperless_redis_hostname }}:{{ paperless_redis_port }}/{{ paperless_redis_database }}"

# Turn true to use StartTLS (usually on port 587)
paperless_email_use_tls: false
# Turn true to use SSL (usually on port 465)
paperless_email_use_ssl: false
paperless_email_host: ""
paperless_email_port: ""
paperless_email_username: ""
paperless_email_password: ""
paperless_email_from: ""

# A list of languages to install OCR support for.
#
# To adjust the list, either redefine `paperless_ocr_languages` or add your values to `paperless_ocr_languages_custom`.
#
# Adjusting this list to a value different than the default will trigger installation of additional `tesseract-ocr-{LANGUAGE}` packages on container startup.
#
# Ref:
# - https://tesseract-ocr.github.io/tessdoc/Data-Files-in-different-versions.html
# - https://docs.paperless-ngx.com/configuration/#PAPERLESS_OCR_LANGUAGES
paperless_ocr_languages: "{{ (paperless_ocr_languages_default + paperless_ocr_languages_auto + paperless_ocr_languages_custom) | unique }}"

# The default list of languages to install OCR support for.
# This matches the default list of languages for which OCR support is already installed.
# Ref:
# - https://tesseract-ocr.github.io/tessdoc/Data-Files-in-different-versions.html
# - https://docs.paperless-ngx.com/configuration/#PAPERLESS_OCR_LANGUAGES
paperless_ocr_languages_default:
  - eng
  - deu
  - ita
  - spa
  - fra

paperless_ocr_languages_auto: []
paperless_ocr_languages_custom: []

# Specify the timezone
paperless_environment_variables_tz: UTC

# Controls the PAPERLESS_OCR_LANGUAGES environment variable.
# Adjusting the list of languages is better done via the `paperless_ocr_languages` variable, not here.
#
# Ref: https://docs.paperless-ngx.com/configuration/#PAPERLESS_OCR_LANGUAGES
paperless_environment_variable_paperless_ocr_languages: "{{ paperless_ocr_languages | join(' ') }}"

# Controls the PAPERLESS_FILENAME_FORMAT environment variable.
# This affects how Paperless stores files in the media directory.
# Leave empty to keep the default settings.
#
# Ref: https://docs.paperless-ngx.com/configuration/#PAPERLESS_FILENAME_FORMAT
paperless_environment_variable_paperless_filename_format: ""

# Additional environment variables to pass to the Paperless-ngx container.
# You can use this to further influence the default configuration.
# Check https://gopaperless.io/docs/installation/configuration for a full configuration reference
#
# Example:
# paperless_variables_additional_variables: |
#   paperless_DISABLE_UPDATE_CHECK=true
paperless_variables_additional_variables: ""

# paperless_restart_necessary controls whether the Paperless-ngx systemd
# services will be restarted (when true) or merely started (when false) by the
# systemd service manager role (when conditional restart is enabled).
#
# This value is automatically computed during installation based on whether
# any configuration files, the systemd service file, or the container image changed.
# The default of `false` means "no restart needed" — appropriate when the role's
# installation tasks haven't run (e.g., due to --tags skipping them).
paperless_restart_necessary: false

########################################################################
#                                                                      #
# tika                                                                 #
#                                                                      #
########################################################################

# Project source code URL: https://github.com/apache/tika

paperless_tika_enabled: false

paperless_tika_identifier: "{{ paperless_identifier }}-tika"

paperless_tika_endpoint: "{{ paperless_tika_scheme }}://{{ paperless_tika_identifier }}:9998"

paperless_tika_scheme: http

# renovate: datasource=docker depName=apache/tika versioning=semver-coerced
paperless_tika_version: "3.1.0.0"

paperless_tika_container_image: "{{ paperless_tika_container_image_registry_prefix }}apache/tika:{{ paperless_tika_container_image_tag }}"
paperless_tika_container_image_tag: "{{ paperless_tika_version }}"
paperless_tika_container_image_registry_prefix: "{{ paperless_tika_container_image_registry_prefix_upstream }}"
paperless_tika_container_image_registry_prefix_upstream: "{{ paperless_tika_container_image_registry_prefix_upstream_default }}"
paperless_tika_container_image_registry_prefix_upstream_default: docker.io/

# A list of additional "volumes" to mount in the container.
# This list gets populated dynamically at runtime. You can provide a different default value,
# if you wish to mount your own files into the container.
# Contains definition objects like this: `{"type": "bind", "src": "/outside", "dst": "/inside", "options": "readonly"}.
# See the `--mount` documentation for the `docker run` command.
paperless_tika_container_additional_volumes: "{{ paperless_tika_container_additional_volumes_auto + paperless_tika_container_additional_volumes_custom }}"
paperless_tika_container_additional_volumes_auto: []
paperless_tika_container_additional_volumes_custom: []

# paperless_tika_container_labels_additional_labels contains a multiline string with additional labels to add to the container label file.
# See `../templates/tika-labels.j2` for details.
# To add your own custom labels, use `paperless_tika_container_labels_additional_labels_custom`
#
# Example:
# paperless_tika_container_labels_additional_labels_custom:
#   - my.label=1
#   - another.label="here"
paperless_tika_container_labels_additional_labels: "{{ paperless_tika_container_labels_additional_labels_auto + paperless_tika_container_labels_additional_labels_custom }}"
paperless_tika_container_labels_additional_labels_auto: []
paperless_tika_container_labels_additional_labels_custom: []

# Specify the size of the container's tmpfs volume
paperless_tika_container_tmpfs_tmp_size: "{{ paperless_container_tmpfs_tmp_size }}"

# Controls how long to wait for the container to stop gracefully before killing it.
paperless_tika_container_stop_grace_time_seconds: "{{ devture_systemd_docker_base_container_stop_grace_time_seconds }}"

# Controls how long the pre-start cleanup should wait for stale container-name release
# before failing startup explicitly.
paperless_tika_container_cleanup_timeout_seconds: "{{ devture_systemd_docker_base_container_cleanup_timeout_seconds }}"

# Controls the delay between pre-start cleanup retries.
paperless_tika_container_cleanup_retry_delay_seconds: "{{ devture_systemd_docker_base_container_cleanup_retry_delay_seconds }}"

# Shell command used in the systemd pre-start phase to deterministically remove a stale
# container and wait until Docker releases its name.
paperless_tika_container_cleanup_command: |
  i=0;
  while [ "$i" -lt {{ paperless_tika_container_cleanup_timeout_seconds }} ]; do
  {{ devture_systemd_docker_base_host_command_docker }} stop -t {{ paperless_tika_container_stop_grace_time_seconds }} {{ paperless_tika_identifier }} >/dev/null 2>&1 || true;
  {{ devture_systemd_docker_base_host_command_docker }} rm -f {{ paperless_tika_identifier }} >/dev/null 2>&1 || true;
  if {{ devture_systemd_docker_base_host_command_docker }} container inspect {{ paperless_tika_identifier }} >/dev/null 2>&1; then
  i=$((i + 1));
  sleep {{ paperless_tika_container_cleanup_retry_delay_seconds }};
  continue;
  fi;
  if {{ devture_systemd_docker_base_host_command_docker }} info >/dev/null 2>&1; then
  exit 0;
  fi;
  echo "Failed to query Docker daemon while cleaning up {{ paperless_tika_identifier }}" >&2;
  exit 1;
  done;
  echo "Failed to remove stale container {{ paperless_tika_identifier }} after {{ paperless_tika_container_cleanup_timeout_seconds }} attempts" >&2;
  exit 1

# A list of extra arguments to pass to the container (`docker create` command)
paperless_tika_container_extra_arguments: "{{ paperless_tika_container_extra_arguments_auto + paperless_tika_container_extra_arguments_custom }}"
paperless_tika_container_extra_arguments_auto: []
paperless_tika_container_extra_arguments_custom: []

# Additional environment variables.
paperless_tika_variables_additional_variables: ""

########################################################################
#                                                                      #
# /tika                                                                #
#                                                                      #
########################################################################

########################################################################
#                                                                      #
# gotenberg                                                            #
#                                                                      #
########################################################################

# Project source code URL: https://github.com/gotenberg/gotenberg

paperless_gotenberg_enabled: false

paperless_gotenberg_identifier: "{{ paperless_identifier }}-gotenberg"

paperless_tika_gotenberg_endpoint: "{{ paperless_gotenberg_scheme }}://{{ paperless_gotenberg_identifier }}:3000"

paperless_gotenberg_scheme: http

# renovate: datasource=docker depName=gotenberg/gotenberg versioning=semver
paperless_gotenberg_version: "8.27.0"

paperless_gotenberg_container_image: "{{ paperless_gotenberg_container_image_registry_prefix }}gotenberg/gotenberg:{{ paperless_gotenberg_container_image_tag }}"
paperless_gotenberg_container_image_tag: "{{ paperless_gotenberg_version }}"
paperless_gotenberg_container_image_registry_prefix: "{{ paperless_gotenberg_container_image_registry_prefix_upstream }}"
paperless_gotenberg_container_image_registry_prefix_upstream: "{{ paperless_gotenberg_container_image_registry_prefix_upstream_default }}"
paperless_gotenberg_container_image_registry_prefix_upstream_default: docker.io/

# A list of additional "volumes" to mount in the container.
# This list gets populated dynamically at runtime. You can provide a different default value,
# if you wish to mount your own files into the container.
# Contains definition objects like this: `{"type": "bind", "src": "/outside", "dst": "/inside", "options": "readonly"}.
# See the `--mount` documentation for the `docker run` command.
paperless_gotenberg_container_additional_volumes: "{{ paperless_gotenberg_container_additional_volumes_auto + paperless_gotenberg_container_additional_volumes_custom }}"
paperless_gotenberg_container_additional_volumes_auto: []
paperless_gotenberg_container_additional_volumes_custom: []

# paperless_gotenberg_container_labels_additional_labels contains a multiline string with additional labels to add to the container label file.
# See `../templates/gotenberg-labels.j2` for details.
# To add your own custom labels, use `paperless_gotenberg_container_labels_additional_labels_custom`
#
# Example:
# paperless_gotenberg_container_labels_additional_labels_custom:
#   - my.label=1
#   - another.label="here"
paperless_gotenberg_container_labels_additional_labels: "{{ paperless_gotenberg_container_labels_additional_labels_auto + paperless_gotenberg_container_labels_additional_labels_custom }}"
paperless_gotenberg_container_labels_additional_labels_auto: []
paperless_gotenberg_container_labels_additional_labels_custom: []

# Specify the size of the container's tmpfs volume
paperless_gotenberg_container_tmpfs_tmp_size: "{{ paperless_container_tmpfs_tmp_size }}"

# Controls how long to wait for the container to stop gracefully before killing it.
paperless_gotenberg_container_stop_grace_time_seconds: "{{ devture_systemd_docker_base_container_stop_grace_time_seconds }}"

# Controls how long the pre-start cleanup should wait for stale container-name release
# before failing startup explicitly.
paperless_gotenberg_container_cleanup_timeout_seconds: "{{ devture_systemd_docker_base_container_cleanup_timeout_seconds }}"

# Controls the delay between pre-start cleanup retries.
paperless_gotenberg_container_cleanup_retry_delay_seconds: "{{ devture_systemd_docker_base_container_cleanup_retry_delay_seconds }}"

# Shell command used in the systemd pre-start phase to deterministically remove a stale
# container and wait until Docker releases its name.
paperless_gotenberg_container_cleanup_command: |
  i=0;
  while [ "$i" -lt {{ paperless_gotenberg_container_cleanup_timeout_seconds }} ]; do
  {{ devture_systemd_docker_base_host_command_docker }} stop -t {{ paperless_gotenberg_container_stop_grace_time_seconds }} {{ paperless_gotenberg_identifier }} >/dev/null 2>&1 || true;
  {{ devture_systemd_docker_base_host_command_docker }} rm -f {{ paperless_gotenberg_identifier }} >/dev/null 2>&1 || true;
  if {{ devture_systemd_docker_base_host_command_docker }} container inspect {{ paperless_gotenberg_identifier }} >/dev/null 2>&1; then
  i=$((i + 1));
  sleep {{ paperless_gotenberg_container_cleanup_retry_delay_seconds }};
  continue;
  fi;
  if {{ devture_systemd_docker_base_host_command_docker }} info >/dev/null 2>&1; then
  exit 0;
  fi;
  echo "Failed to query Docker daemon while cleaning up {{ paperless_gotenberg_identifier }}" >&2;
  exit 1;
  done;
  echo "Failed to remove stale container {{ paperless_gotenberg_identifier }} after {{ paperless_gotenberg_container_cleanup_timeout_seconds }} attempts" >&2;
  exit 1

# A list of extra arguments to pass to the container (`docker create` command)
paperless_gotenberg_container_extra_arguments: "{{ paperless_gotenberg_container_extra_arguments_auto + paperless_gotenberg_container_extra_arguments_custom }}"
paperless_gotenberg_container_extra_arguments_auto: []
paperless_gotenberg_container_extra_arguments_custom: []

# Additional environment variables.
paperless_gotenberg_variables_additional_variables: ""

########################################################################
#                                                                      #
# /gotenberg                                                           #
#                                                                      #
########################################################################

########################################################################
#                                                                      #
# metrics                                                              #
#                                                                      #
########################################################################

paperless_metrics_enabled: false
paperless_metrics_path_prefix: '/metrics'
paperless_metrics_container_labels_traefik_rule: "Host(`{{ paperless_hostname }}`) && PathPrefix(`{{ paperless_metrics_path_prefix | quote }}`)"
paperless_metrics_container_labels_traefik_entrypoints: web-secure
paperless_metrics_container_labels_traefik_tls: "{{ paperless_metrics_container_labels_traefik_entrypoints != 'web' }}"
paperless_metrics_container_labels_traefik_tls_certResolver: default  # noqa var-naming

########################################################################
#                                                                      #
# /metrics                                                             #
#                                                                      #
########################################################################
